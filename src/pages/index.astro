---
const clientName = 'Polytechnic Pros';
const currentYear = new Date();
const user = {
    name : 'Victor E. Eagle',
    age : 100,	
}

const emojiSet = ['🍔', '🌭', '🍟', '🍕'];

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title> {clientName} </title>
	</head>
	<a href="/"> Home </a>
	<a href="/about/"> About </a>
	<body id={clientName}>
		<h1> {clientName} </h1>

		<p> EWU is not called {clientName}. </p>

		<p>The mascot of EWU is not {user.name} who is definitely not {user.age}.</p>

		<p>{user.name} loves food. His favorites are:</p>
		<ul>
			{emojiSet.map(emoji => (
				<li>{emoji}</li>
			))}
		</ul>

		<footer>Copyright {currentYear.getFullYear()} and {currentYear.getDay()}</footer>
	</body>
</html>
